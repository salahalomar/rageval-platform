# Copy to .env before `make dev`. .env is gitignored; this file is committed.

# --- Postgres ---------------------------------------------------------------
POSTGRES_USER=rag
POSTGRES_PASSWORD=rag
POSTGRES_DB=rag
POSTGRES_PORT=5432

# Host-side tooling (make migrate, pytest) talks to the published port. The API
# container gets its own DATABASE_URL from docker-compose.yml, pointing at the
# `postgres` service name instead.
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag

# --- API --------------------------------------------------------------------
API_PORT=8000
LOG_LEVEL=INFO

# --- Generation -------------------------------------------------------------
# Required by POST /query and GET /query/stream. Everything else in the system —
# ingestion, retrieval, reranking, refusal, and the entire test suite — runs without
# it. Never commit a real value.
ANTHROPIC_API_KEY=
