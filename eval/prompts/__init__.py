"""Versioned prompts for the evaluation harness.

Kept as files for the same reason the generation prompt is: a prompt change moves the
numbers, so it must be nameable in a result record and diffable in review.
"""
